shows_root: "examples/shows"

naming:
  frame_prefix: "frame_"
  frame_padding: 4
  frame_ext: ".exr"

thresholds:
  disk_warning_mb: 500

tracking:
  backend: "json"
  json_path: "data/tracking_db.json"

publishing:
  publish_root: "published"
